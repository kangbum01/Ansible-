---
- name: 사용자 추가
  hosts: web1.example.com
  vars_files:
    - secret.yml
  tasks:
    - name: Secret 사용한 usre1 사용자 추가
      ansible.builtin.user:
        name: "{{ username }}"
        password: "{{ pwhash }}"